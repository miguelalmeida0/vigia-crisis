import { immutable,semanticHash } from '../intelligence/shared.mjs';

const ALLOWED=new Set(['PREFETCH_CONTEXT','PRIORITIZE_MONITORING','RECORD_ABSTENTION','REQUEST_MISSING_INPUTS']);
const CONSEQUENTIAL=new Set(['EVACUATE','DISPATCH','SUPPRESSION_ALLOCATION','ROAD_CLOSURE','PUBLIC_WARNING','RESOURCE_REALLOCATION']);
export function createForecastPolicyInput({forecast,impact=null,requestedActions=[]}={}){const allowed=[],blocked=[];for(const action of requestedActions.map(String)){if(ALLOWED.has(action))allowed.push(action);else blocked.push({action,reason:CONSEQUENTIAL.has(action)?'CONSEQUENTIAL_ACTION_FORBIDDEN':'ACTION_NOT_ALLOWLISTED'});}const core={schemaVersion:'vigia.forecast-shadow-policy-input.v1',forecastId:forecast.forecastId,incidentId:forecast.incidentId,epistemicType:'FORECAST',evidenceEligible:false,authorityEligible:false,forecastState:forecast.state,qualityState:forecast.qualityState,possibleExposureCount:impact?.exposures?.length??0,consequentialExecution:'DISABLED',allowedActions:[...new Set(allowed)].sort(),blockedActions:blocked.sort((a,b)=>a.action.localeCompare(b.action))};return immutable({...core,policyInputHash:semanticHash('forecast-shadow-policy-input',core)});}
