# Capture requirements

The previous README gallery was captured while the console reported unavailable data/maps. Its PNG files are retained for diagnosis, not promoted as successful product demonstrations.

A replacement session must show a populated incident set, a loaded map, current page identity and no unavailable-data banner. Record the commit, route, capture date and data source. Synthetic demo records must be labeled. A successful screenshot write alone is not a passing application check.
