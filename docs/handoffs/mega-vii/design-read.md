# Mega VII design read

VIGIA is operational wildfire decision support for an operator under time pressure. The primary task is understanding qualified support, shared access and what remains under explicit failure assumptions. The existing dynamic incident map remains the dominant object.

Direction: one map and an aligned operational rail, using DESIGN.md navy text, white surfaces, blue calculated paths, amber assumed loss, red incident/selection, hairline separators and existing buttons. Design variance 2/10; motion 1/10; density 7/10; operational risk. User-approved VII composition supersedes nearest-support anatomy only within its scope.

P1: Operational Picture action and named qualified hospital/fire response. P2: shared road and community relationships with direct selection and Test unavailability. P3: causal change, replay, stress test and gaps through a compact mode row. Source details stay subordinate; consequences do not.

Response & Access replaces geographic-nearest ranking with the operational rail beside the existing map. Detail exposes the same mode from its toolbar and a map-adjacent rail. Raw mapped facilities remain accessible as geographic context. No new primary routes. Unavailable retained support yields an inline retry and explicit absence, never a weather-only substitute.

Desktop: dominant map plus readable 360–440px rail. Tablet: map then rail with concise support rows. Mobile: controls, usable map, then same named support rows; no squeezed tables or hidden primary intelligence. Touch controls 44px, keyboard selection and visible focus. Scenario, historical and current states are mutually identified; exiting restores the existing map scene. Replay pauses on selection/navigation and never merges current caches into a historical snapshot.

All map features come from backend snapshot geometry and qualified relationships. One coverage category at a time; point/path bands only. Failure maps retain lost routes as dashed assumptions and show remaining qualified options. Significant corridors use actual route-step geometry when retained, otherwise clearly labeled whole-route relationships; no fabricated road centerline.

Validation: controlled domain invariants and canonical browser interaction captures are separate. Review eight repository viewports, map restoration, race handling, zero model calls during ordinary map use, direct road failure, multi-failure, community, coverage degradation, replay, stress, gap and Ask-map workflows. Fresh-session scripted discoverability is measured separately from an independent human user test.
