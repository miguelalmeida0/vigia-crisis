# Mermaid architecture sources

These `.mmd` files are the canonical diagram sources mirrored into the repository README. GitHub renders the README blocks natively with its interactive Mermaid viewer.
