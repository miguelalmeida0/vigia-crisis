export function createForecastConsumerSnapshot({forecast,inputManifest,impact,shadow,replay,priorForecast=null}={}){return Object.freeze({schemaVersion:'vigia.forecast-consumer-snapshot.v1',forecastAvailability:forecast.state==='ABSTAINED'?'UNAVAILABLE':'AVAILABLE',forecastStatus:forecast.state,issuedAt:forecast.issuedAt,informationCutoff:forecast.informationCutoff,incidentId:forecast.incidentId,forecastType:forecast.forecastType,horizons:forecast.horizons.map((item)=>({hours:item.hours,validAt:item.validAt,probabilityContours:item.contours,expectedPerimeter:item.expectedPerimeter,probabilitySurfaceRef:item.probabilitySurfaceRef})),uncertainty:{kind:forecast.horizons.length?'PROBABILITY_THRESHOLD_CONTOURS':'UNAVAILABLE',calibrationStatus:forecast.calibrationRef?'CALIBRATED':'UNCALIBRATED',warning:'An outer contour is a possibility envelope, not a statement that fire will reach it.'},abstention:forecast.abstention,inputFreshness:{manifestHash:inputManifest.semanticHash,weatherMembers:inputManifest.weatherMembers.length,weatherRun:inputManifest.weatherMembers[0]?.run??null,weatherIssueTime:inputManifest.weatherMembers[0]?.issueTime??null,initialPerimeterVersion:inputManifest.initialPerimeter.version,initialPerimeterObservedAt:inputManifest.initialPerimeter.observedAt,initialPerimeterKnowledgeTime:inputManifest.initialPerimeter.knowledgeTime,initialPerimeterAgeAtIssueMs:inputManifest.initialPerimeter.observedAt?Date.parse(forecast.issuedAt)-Date.parse(inputManifest.initialPerimeter.observedAt):null,fuelDataset:inputManifest.fuelDataset?.datasetId??null,terrainDataset:inputManifest.terrainDataset?.datasetId??null},model:forecast.model,provenance:{forecastId:forecast.forecastId,inputManifestHash:forecast.inputManifestHash,replayFingerprint:forecast.replayFingerprint},possibleAssetExposures:impact?.exposures??[],timeToExposureRanges:(impact?.exposures??[]).map((item)=>({assetId:item.assetId,earliestPlausibleHours:item.earliestPlausibleHours,medianThresholdHours:item.medianThresholdHours,highProbabilityHours:item.highProbabilityHours})),changeFromPrior:priorForecast?{priorForecastId:priorForecast.forecastId,priorIssuedAt:priorForecast.issuedAt,stateChanged:priorForecast.state!==forecast.state,modelChanged:priorForecast.model.fingerprint!==forecast.model.fingerprint,inputChanged:priorForecast.inputManifestHash!==forecast.inputManifestHash,outputChanged:priorForecast.outputHash!==forecast.outputHash}:null,qualityWarnings:forecast.qualityWarnings,limitations:forecast.limitations,shadow:{mode:shadow.mode,consequentialExecution:shadow.consequentialExecution},replay:{valid:replay.valid,identical:replay.identical},semanticDisplayRules:[{type:'OBSERVED',label:'Observed perimeter',styleRequirement:'solid boundary plus text label'},{type:'FORECAST_EXPECTED',label:'Forecast median / expected progression',styleRequirement:'patterned boundary plus forecast label'},{type:'FORECAST_UNCERTAINTY',label:'Uncertainty envelope',styleRequirement:'distinct pattern and probability meaning; never color alone'},{type:'WEATHER_CONTEXT',label:'Context-only weather',styleRequirement:'context label; never fire evidence'},{type:'POTENTIAL_EXPOSURE',label:'Potential exposure',styleRequirement:'conditional language'},{type:'CONFIRMED_EXPOSURE',label:'Confirmed exposure',styleRequirement:'requires attributable observation'}]});}
