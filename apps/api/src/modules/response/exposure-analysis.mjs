import { pointInPolygon } from '../../../../../packages/domain/src/geo.mjs';
function count(points, feature){return (points??[]).filter((point)=>pointInPolygon(point,feature)).length;}
function assetCounts(assets,feature){const matched=(assets??[]).filter((item)=>pointInPolygon(item.coordinate,feature));const byKind={};for(const item of matched)byKind[item.kind]=(byKind[item.kind]??0)+1;return{count:matched.length,byKind,assets:matched.slice(0,40)};}
export function analyseSpreadExposure(exposure,spread){if(!exposure||!spread?.envelopes?.length)return null;const buildings=exposure.buildingPoints??[],assets=exposure.assets??[];return spread.envelopes.map((entry)=>{const low=entry.low??entry.central,central=entry.central,high=entry.high??entry.outer;return{minutes:entry.minutes,low:{buildings:count(buildings,low),...assetCounts(assets,low)},central:{buildings:count(buildings,central),...assetCounts(assets,central)},high:{buildings:count(buildings,high),...assetCounts(assets,high)},sampledBuildingPoints:buildings.length,reportedBuildingCount:exposure.buildingCountWithin3Km??null};});}
